White 16px-radius card with warm shadow; `image` renders a cover strip. Clickable when `onClick` set (hover raises).
```jsx
<Card image="/photos/kitchen.jpg" onClick={open}><h3>Anita's Kitchen</h3></Card>
```