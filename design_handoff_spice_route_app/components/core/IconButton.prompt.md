Circular icon-only button (back arrows, cart, favorites); pass a Lucide SVG as the child and always set `label`.
```jsx
<IconButton label="Cart" variant="tonal"><CartIcon/></IconButton>
```
Variants: ghost, tonal, solid. Default 40px (≥44px touch area with spacing).