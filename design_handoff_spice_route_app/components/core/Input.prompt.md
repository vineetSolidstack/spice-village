Labeled text input; 12px radius, focus ring via border color, error state via `error` string.
```jsx
<Input label="Kitchen name" placeholder="Anita's Kitchen" hint="Shown to customers"/>
<Input label="Phone" error="Enter a 10-digit number"/>
```