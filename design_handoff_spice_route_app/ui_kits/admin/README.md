# Kitchen Owner (Admin) UI Kit
Mobile portal (390px): Dashboard (open/closed badge, new/preparing/sales stats, needs-attention orders), Orders (status pipeline New → Preparing → Ready → verify QR → Completed), Menu (availability switches, edit/delete, add item), Settings (accepting-orders toggle, kitchen profile). Functional copy — no puns (admin surface).
